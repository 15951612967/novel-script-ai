PORT=8787

# 可选：默认模型来源。页面里也可以选择模型来源。
# 可选值：auto、dashscope、openai、mock
# AI_PROVIDER=mock

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# 阿里云百炼 / DashScope，使用 OpenAI-compatible endpoint
DASHSCOPE_API_KEY=
# 强模型默认用 qwen-max；如需省额度可改 qwen-plus
DASHSCOPE_MODEL=qwen-max
